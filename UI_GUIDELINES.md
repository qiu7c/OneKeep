# OneKeep iOS 16 界面与图标规范（V0.1）

## 1. 设计目标

- 训练中单手可操作。
- 当前动作、次数、重量和剩余时间一眼可见。
- 尽量减少文字输入和页面跳转。
- 深色环境和户外环境下都保持足够对比度。
- 所有界面禁止使用 Emoji 充当按钮、状态或装饰图标。
- 整体简洁、轻快、适合年轻用户，不采用健身房常见的厚重工业风。
- 全局禁止渐变色、渐变遮罩和渐变文字。
- 装饰克制，视觉重点来自排版、留白、纯色和训练数据本身。

## 1.1 视觉语言

- 背景使用纯色，不叠加纹理或大面积装饰图形。
- 每个页面只保留一个主要操作按钮。
- 卡片采用轻量圆角和细边框，避免层层嵌套卡片。
- 阴影尽量不用；需要区分层级时优先使用背景色差和分隔线。
- 使用清晰的大标题、紧凑的数据字体和短句，不展示冗长说明。
- 年轻感通过鲜明但克制的强调色、动效节奏和简洁图标表达，不使用霓虹渐变或拟物装饰。
- 浅色和深色主题均使用纯色调色板。

## 2. 图标系统

### 基础图标

优先使用 iOS 16 自带的 SF Symbols：

- 导航、返回、添加、编辑、删除、搜索。
- 播放、暂停、停止、计时和提醒。
- 日历、历史、设置、照片和视频。
- 重量、趋势、收藏和下载状态。

每个图标必须有可访问性标签，不能只靠图形表达关键含义。

### 自绘图标

SF Symbols 缺少的健身动作和训练结构图标采用自绘矢量资源：

- 使用统一 24 × 24 设计网格。
- 默认 2 pt 圆角描边。
- 端点和转角风格统一。
- 同时提供常规和强调状态。
- 导出为单色 PDF Vector 或 SVG 源文件。
- 颜色由 SwiftUI `foregroundStyle` 控制，不在资源内写死。
- 不绘制过细的人体解剖细节，小尺寸下只保留动作轮廓。

首批需要自绘的图标：

- 热身。
- 力量训练。
- 循环训练。
- 间歇训练。
- 组间休息。
- 次数型动作。
- 计时型动作。
- 左右侧动作。
- 体重记录。
- 围度记录。

## 3. 训练执行器

主界面信息优先级：

1. 当前动作名称和演示视频。
2. 当前第几组/共几组。
3. 计划与实际次数、重量或时长。
4. 完成本组主按钮。
5. 休息计时及增减时间。
6. 上一动作、下一动作和动作详情。

规则：

- 主按钮高度不少于 56 pt。
- 关键点击区域不少于 44 × 44 pt。
- 计时数字使用等宽数字，避免跳动。
- 重量和次数使用数字键盘与步进按钮。
- 默认显示上次训练数据，但不自动覆盖本次计划值。
- 视频默认静音循环，允许暂停和全屏。
- 在线视频显示加载、缓冲、失败重试和外部打开状态，不使用 Emoji 表达状态。
- 网页视频默认在 App 内嵌播放；播放器保持固定比例，进入全屏前不遮挡组次和计时状态。
- 外部打开按钮仅在内嵌失败或站点禁止播放时出现。
- 切换动作详情时计时继续，不重置状态。

## 4. 简短记录界面

记录类型：饮食、睡眠、身体数据、普通备注。

- 饮食：时间 + 一行文字 + 可选照片。
- 睡眠：入睡和起床时间 + 可选备注。
- 身体：选择指标 + 数值 + 单位。
- 不展示评分、红黄绿判断或自动建议。
- 列表使用统一线性图标，不使用食物或睡眠 Emoji。

## 5. 颜色和状态

- 主色仅用于主要操作和当前进度。
- 成功、警告和错误不能只靠颜色表达，必须搭配图标和文字。
- 支持浅色、深色和系统高对比度模式。
- 动作视频之上的文字使用纯色半透明背景保证可读性，禁止渐变遮罩。
- 每个主题只设置一个品牌强调色和一组语义色。
- 大面积背景使用中性色；高饱和颜色只用于进度、选中状态和主要按钮。

确定使用低调的中性灰品牌体系，不设置高饱和品牌色。

浅色模式建议色板：

- 页面背景：`#F5F5F3`。
- 卡片/浮层：`#FFFFFF`。
- 主要文字与主要按钮：`#1C1C1E`。
- 次要文字：`#6E6E73`。
- 分隔线和边框：`#D8D8DC`。
- 次级填充：`#ECECEE`。

深色模式建议色板：

- 页面背景：`#0F0F10`。
- 卡片/浮层：`#1C1C1E`。
- 主要文字：`#F2F2F7`。
- 次要文字：`#A1A1A6`。
- 分隔线和边框：`#38383A`。
- 主要按钮：`#D1D1D6`，按钮文字使用深灰。

使用规则：

- 主要操作依靠黑白反差、字重和空间层级突出，不依赖彩色按钮。
- 训练进度、选中状态和计时环使用石墨灰或反色，不使用彩色渐变。
- 红、橙等颜色只用于错误、危险、警告等语义状态，不作为品牌装饰。
- 成功状态优先使用勾选图标和文字；如需颜色，仅使用低饱和系统绿色。
- 所有最终色值在原型和真机上进行浅色、深色与高对比度测试后微调。

## 6. 字体和无障碍

- 使用系统字体和 Dynamic Type。
- 训练计时数字建议使用 `.monospacedDigit()`。
- 支持 VoiceOver 朗读动作、组数、重量和剩余时间。
- “完成一组”等关键按钮提供明确的 VoiceOver Hint。
- 减少动态效果开启时，禁用不必要的缩放和位移动画。

## 6.1 动效规范

- 动效目标是确认操作结果、解释页面关系和维持计时连续感，不做纯装饰动画。
- 普通按钮反馈控制在约 100～160 ms。
- 页面元素切换控制在约 180～280 ms。
- 弹层使用轻微缩放和淡入；列表更新使用短距离位移。
- 完成一组时给予一次简短触觉反馈和状态切换，不播放庆祝粒子或 Emoji 动画。
- 休息计时开始时数字平滑切换，进度环使用纯色描边，不使用渐变。
- 避免同时为多个大面积元素添加模糊、缩放和透明度动画。
- 动画期间禁止触发重复数据库写入和网络请求。
- 使用系统动画能力，避免持续运行的自定义计时重绘。
- 在 60Hz 设备保持稳定；ProMotion 设备由系统自然提升刷新率，不写死帧率。
- 开启“减少动态效果”后，将位移和缩放替换为简短淡入淡出。

## 7. 禁止事项

- 禁止 Emoji 图标。
- 禁止任何渐变元素。
- 禁止从多个风格不同的图标包混用素材。
- 禁止用无文字的神秘图标承载不可逆操作。
- 禁止训练时出现大段说明遮挡主操作。
- 禁止仅为装饰加入连续动画。
- 禁止大面积玻璃拟态、霓虹光效、强阴影和高频弹跳动画。
- 禁止每个区块都使用不同强调色，避免界面像数据看板或游戏活动页。
