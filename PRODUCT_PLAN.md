# OneKeep iOS 产品与开发计划（V0.3）

## 1. 已确定的产品方向

OneKeep 是一款 iOS 训练执行辅助工具，不负责替用户制定训练方案。

用户可以：

1. 手动填写自己的训练日程。
2. 在其他 AI 中生成计划后，复制到 OneKeep 导入。
3. 在 OneKeep 内填写自己的 AI 服务地址、模型和 API Key，让 AI 整理计划。
4. 将整理后的计划预览并一键写入训练日程。
5. 训练时快速查看动作、组次要求和注意事项，并使用休息/动作计时器。

产品重点按优先级排列：

1. 训练时操作简单、字大、信息清晰。
2. 动作查看方便。
3. 计时可靠，切到后台或锁屏后仍能正确恢复。
4. 任意来源的计划可以低成本导入。
5. 本地数据可靠，不强制注册账号。
6. 饮食、睡眠和身体数据只做简短记录，不提供管理目标或健康建议。

## 2. 平台与安装范围

- 只开发 iPhone 客户端。
- 最低支持 iOS 16.0。
- 原生技术栈：Swift、SwiftUI、Swift Concurrency。
- iOS 16 数据持久化：Core Data，不使用 iOS 17 才提供的 SwiftData。
- iOS 16 状态管理：`ObservableObject`、`@StateObject`、`@Published`。
- 首版优先支持 TrollStore，其他自签/侧载方式后续验证。
- GitHub Actions 使用 macOS Runner 云端编译和打包。
- 发布任务默认提供可供后续签名的 IPA；未来再增加 TestFlight/App Store 流程。
- 首版不开发 Android、Web、小程序、iPad 专属界面或 Apple Watch App。

## 3. 核心用户流程

### 3.1 建立计划

首页 → 新建计划 → 选择录入方式：

- 手动录入。
- 粘贴文本并解析。
- 使用已配置的 AI 整理。

AI 整理流程：

粘贴或描述计划 → 发送到用户配置的兼容接口 → 返回结构化计划 → 本地校验 → 预览 → 用户确认 → 一键写入日程。

AI 返回内容不得直接静默保存。必须先展示预览和校验结果，避免错误的日期、组数或动作覆盖用户数据。

### 3.2 开始训练

今日 → 选择训练日 → 开始训练 → 查看当前动作 → 完成一组 → 自动开始休息计时 → 下一组/下一动作 → 完成训练 → 保存实际记录。

### 3.3 临时训练

首页 → 快速训练 → 搜索或新建动作 → 添加组数和计时 → 开始。

临时训练结束后可以保存为可复用模板。

## 4. MVP 功能范围

### P0：必须具备

#### 计划与日程

- 手动创建、编辑、复制和删除训练计划。
- 计划包含多个训练日，每个训练日包含有序动作列表。
- 训练日支持指定日期、每周循环、隔周循环和计划结束日期。
- 周期日程使用起始日期作为锚点，支持跳过单次训练或只修改某次训练，不影响整个循环。
- 动作支持目标组数、次数区间、重量、持续时间和休息时间。
- 支持从纯文本粘贴内容。
- 支持从 JSON 计划文件导入，并支持将单个或多个计划导出为 JSON。
- AI 解析成功后提供结构化预览和一键导入。
- 导入失败时保留原始文本并提示具体错误。

#### 动作查看

- 动作名称、别名、目标肌群、器械和动作类型。
- 图文动作步骤。
- 常见错误和安全提示。
- 首版支持在线视频预览播放，并提供封面图和文字步骤作为无网络/播放失败时的兜底。
- 原生视频源优先支持 MP4 直链和 HLS（`.m3u8`），进入详情后可边加载边播放。
- 视频预览默认静音循环；用户可暂停、拖动、全屏和重新加载。
- 已观看视频可按缓存策略保存在本地，设置中可查看和清理视频缓存。
- 训练界面可点开动作详情，不丢失当前计时和训练进度。
- 用户可创建自定义动作，并填写说明或媒体链接。
- 动作搜索支持中文名、英文名和别名。

#### 训练执行器

- 当前动作和下一动作清晰可见。
- 一键完成当前组。
- 自动进入组间休息倒计时。
- 增减时间、暂停、跳过、重新开始。
- 支持按时间训练的动作倒计时。
- 支持正计时，用于平板支撑以外的自由时长项目。
- 震动、提示音和本地通知提醒。
- 切后台或锁屏时保存目标结束时间，回来后根据系统时间恢复，而不是依赖持续运行的内存计时器。
- 训练中每次操作即时保存，意外退出后可继续。
- 可临时修改本组次数、重量和休息时间，不强制修改原计划。
- 重量是核心记录项：每组分别保存计划重量和实际重量，并可快速带入上次训练重量。

#### 历史记录

- 训练日期、总时长、完成动作和组数。
- 每组实际次数、重量或持续时间。
- 查看某个动作的历史表现。
- 查看重量趋势、个人最佳重量和同重量下的最高次数。
- 可以从历史训练再次开始或复制为模板。

#### 本地设置

- 公斤/磅单位。
- 默认提示音、震动和自动开始休息设置。
- 数据导出和备份入口。
- 无账号也能完整使用除 AI 外的所有功能。

### P1：验证后加入

- iCloud/CloudKit 同步。
- Live Activity 和灵动岛计时展示（需按系统版本提供）。
- 锁屏小组件。
- HealthKit 写入训练记录。
- 从截图或图片 OCR 导入计划。
- URL Scheme/Share Extension，从其他 AI App 分享计划到 OneKeep。
- 计划导入模板市场或社区分享。

### 首版不做

- 内置固定的计划生成算法。
- 强制注册和自建用户后台。
- 社区、直播、商城和教练撮合。
- 摄像头实时姿态识别。
- 医疗诊断、康复处方或保证训练效果。

## 5. 页面结构

底部 3 个 Tab：

1. 今日
   - 今天安排的训练。
   - 快速开始临时训练。
   - 继续未完成训练。

2. 计划
   - 计划列表。
   - 周日程/日期日程。
   - 手动编辑、粘贴导入、AI 整理。

3. 我的
   - 训练历史。
   - 动作库。
   - 简短记录。
   - AI 服务设置。
   - 计时、单位、备份和关于设置。

训练执行器使用全屏界面，降低误触并隐藏无关导航。

## 6. 动作库设计

### 内置动作字段

- 唯一 ID。
- 中文名、英文名和别名。
- 动作类型：次数型、计时型、自由计时型。
- 主要/辅助肌群。
- 器械。
- 动作步骤。
- 常见错误。
- 安全提示。
- 媒体资源。
- 素材作者、原始页面、许可证、署名文本和校验日期。

### 自定义动作

用户可以创建动作并提供：

- 名称和别名。
- 简短说明。
- 默认组数、次数/时长和休息时间。
- 视频只支持外部链接，不支持从相册选择或保存本地视频文件。

AI 导入遇到动作库不存在的名称时：

1. 先用名称和别名匹配已有动作。
2. 展示建议匹配，允许用户更换。
3. 无匹配时创建“待完善的自定义动作”，不能丢弃该动作。

## 6.1 简短生活记录

饮食、睡眠和身体数据不是 OneKeep 的管理模块，只提供低成本记录：

- 饮食：时间、简短文字、可选照片。
- 睡眠：入睡时间、起床时间、自动计算时长、可选备注。
- 身体：体重、身高、腰围、肩宽和可选体态照片。
- 通用备注：最多一段简短文字。

明确不做：

- 热量和营养素计算。
- 菜谱、饮食目标、饮食评分或饮食建议。
- 睡眠目标、睡眠评分、智能分析或催睡提醒。
- 根据身材数据自动修改训练计划。
- 对体型变化作效果保证。

记录入口放在“我的 → 简短记录”，首页可提供一个快速新增按钮，但不占据训练主流程。

## 7. AI 兼容接口设计

### 用户配置

- 服务名称。
- Base URL，例如服务商的 `/v1` 地址。
- API Key。
- 模型名称，允许用户手动填写。
- 可选超时时间。
- “测试连接”按钮。

API Key 只保存在 iOS Keychain；界面只显示掩码，不写入日志、Core Data、导出文件或崩溃报告。

### MVP 兼容范围

MVP 将“OpenAI 兼容协议”明确限定为 Chat Completions 风格的 JSON HTTP 接口：

- Bearer Token 鉴权。
- 可配置 Base URL。
- `model`、`messages` 等常见字段。
- 要求模型只返回指定的计划 JSON。

不同服务商并不保证完全兼容，因此客户端需要：

- 宽容处理 Markdown 代码块包裹的 JSON。
- 校验必填字段、数值范围和日期。
- 展示服务端原始错误，但隐藏 Key 和敏感请求头。
- 对解析失败提供重试和手动修正。
- 保存用户原始输入，不因失败而丢失。

MVP 不承诺兼容每家服务的私有参数、流式事件格式、工具调用或多模态图片输入。

### 计划 JSON 草案

```json
{
  "title": "每周三练",
  "schedule": [
    {
      "name": "训练 A",
      "weekdays": [1, 4],
      "exercises": [
        {
          "name": "深蹲",
          "sets": 4,
          "reps": "8-10",
          "weight": null,
          "durationSeconds": null,
          "restSeconds": 90,
          "notes": "保持核心收紧"
        }
      ]
    }
  ]
}
```

解析器会把 AI JSON 转换为内部模型；内部数据模型不直接依赖任何 AI 服务商的响应格式。

## 8. iOS 16 技术架构

- UI：SwiftUI。
- 状态：`ObservableObject`、`@StateObject`、`@EnvironmentObject`。
- 数据库：Core Data，启用轻量迁移。
- Key：Security/Keychain Services。
- 网络：URLSession + async/await。
- 通知：UserNotifications。
- 音频：AVFoundation。
- 震动：Core Haptics，并提供不支持设备的降级。
- 媒体：本地 Asset/文件目录；远程媒体使用 URLSession 缓存。
- 视频播放：AVKit/AVPlayer；MP4 与 HLS 在线预览，远程视频按需缓存，离线时回退到已缓存视频、封面图和文字步骤。
- 网页视频：使用独立的 `WebVideoSource` 类型隔离，不把网页地址误当作 MP4 交给播放器。
- 哔哩哔哩、YouTube 等网页视频优先使用受控 `WKWebView` 在 OneKeep 内嵌播放。
- 针对已支持站点建立 URL 解析器，将普通分享链接转换为对应的嵌入播放地址。
- 平台禁止嵌入、视频下架或加载失败时，才提供“打开原始页面”兜底操作。
- 内嵌播放器使用独立网页数据存储策略，不允许网页任意跳转到未授权域名。
- 日志：OSLog，敏感字段设为 private 或完全不记录。
- 测试：XCTest、XCUITest。

建议工程结构：

```text
OneKeep/
  App/
  Core/
    Database/
    Keychain/
    Networking/
    Timer/
    Media/
    DesignSystem/
  Features/
    Today/
    Plans/
    PlanImport/
    Workout/
    ExerciseLibrary/
    History/
    Settings/
  Resources/
OneKeepTests/
OneKeepUITests/
```

## 9. 核心数据模型

- `TrainingPlan`：计划名称、启用状态、重复规则。
- `TrainingDay`：名称、指定日期或星期规则、动作顺序。
- `ScheduleRule`：锚点日期、间隔周数、星期、结束日期和时区。
- `ScheduleException`：跳过、移动或单独修改某次日程。
- `PlannedExercise`：动作、目标组次/时长、休息、备注。
- `Exercise`：内置或自定义动作资料及媒体。
- `WorkoutSession`：一次实际训练的开始、暂停、完成状态。
- `PerformedSet`：计划/实际次数、计划/实际重量、时长和完成时间。
- `TimerState`：计时类型、目标结束时间、暂停剩余时间。
- `AIProvider`：服务名、Base URL、模型和非敏感设置；Key 单独存 Keychain。
- `ImportDraft`：原始文本、解析 JSON、校验结果和导入状态。
- `QuickLog`：记录类型、时间、简短内容和可选媒体。
- `BodyMeasurement`：体重、身高、腰围、肩宽和记录日期。
- `SleepLog`：入睡、起床、时长和备注。
- `ExportManifest`：导出格式版本、App 版本、创建时间、数据类型和完整性信息。

计划值和训练实际值必须分别保存，修改计划不能改变历史记录。

## 10. GitHub 云构建与 IPA

### `ios-ci.yml`

触发：Pull Request、推送到 `main`。

- Checkout。
- 选择项目支持的 Xcode。
- 解析依赖。
- 编译 iOS 16 目标。
- 运行单元测试和 UI 冒烟测试。
- 不需要 Apple 证书。

### `ios-release.yml`

触发：Git Tag 或手动运行。

- Archive Release 构建。
- 生成 `.app`。
- 将 `.app` 放入 `Payload` 目录并打包成 IPA。
- 上传 GitHub Actions Artifact 和 GitHub Release。
- 文件名包含版本、构建号和提交短 SHA。

发布包建议区分：

- `OneKeep-unsigned.ipa`：供本地自签工具重新签名。
- `OneKeep-trollstore.ipa`：使用标准沙盒权限并通过 `ldid` 假签名，交给 TrollStore 安装时重签。
- `OneKeep-signed.ipa`：未来提供证书和描述文件后可选生成。

证书、API Key 和 Provisioning Profile 不得写入仓库。若未来配置签名，统一放到 GitHub Environment Secrets。

## 11. 开发顺序

### 阶段 1：可训练的本地 MVP

- 建立 iOS 16 SwiftUI 工程和 Core Data 模型。
- 完成手动计划、今日页和训练执行器。
- 完成后台恢复型计时器、声音、震动和通知。
- 完成包含热身的首批动作库、视频播放和动作详情。
- 完成逐组重量记录、上次重量快速带入和历史查询。
- 建立 GitHub 无签名编译与 TrollStore IPA Artifact。

### 阶段 2：计划导入

- 完成纯文本导入和结构化预览。
- 完成 AI 服务配置、Keychain 和连接测试。
- 完成 Chat Completions 风格请求、JSON 校验和一键导入。
- 处理未知动作匹配和自定义动作生成。

### 阶段 3：体验完善

- 历史训练、动作历史和模板复制。
- 饮食、睡眠和身体数据的简短记录。
- JSON 计划文件导入/导出和完整数据备份/恢复。
- 无障碍、深色模式、字体缩放和不同屏幕尺寸适配。
- 评估 Live Activity、HealthKit 和分享扩展。

## 12. 验收重点

- 无网络时除 AI 外所有功能可用。
- App 被杀进程后，未完成训练可以恢复。
- 锁屏五分钟后返回，计时结果仍准确。
- AI 返回错误 JSON 时不会破坏已有计划。
- API Key 不出现在数据库、日志和导出文件中。
- 未签名 IPA 能进入用户选定的侧载签名流程。
- iOS 16 真机可以启动、编辑计划、计时和保存历史。

## 13. 待产品确认

已确认：指定日期、每周/隔周循环、计划结束日期、逐组重量记录、TrollStore 优先、AI 预览可编辑、动作视频支持。

视觉规范已确认：不使用 Emoji；采用完整统一的图标体系，缺失图标使用自绘矢量资源。
界面风格已确认：简洁、年轻、全局无渐变；使用纯色、留白和清晰排版，动效流畅且克制。
品牌视觉已确认：采用低调的中性灰色体系，不使用高饱和品牌强调色。

当前仍需确认：

当前产品范围已经确认，无待决项。

已确认网页视频链接优先在 OneKeep 内嵌播放，外部页面只作为失败兜底。
已确认支持 JSON 计划文件导入/导出和完整数据备份/恢复；动作视频仅支持链接。

## 14. JSON 导入、导出与完整备份

### 文件类型

1. `*.onekeep-plan.json`
   - 用于分享或导入训练计划。
   - 包含日程规则、阶段、动作、组次、重量、计时和备注。
   - 不包含训练历史、生活记录和 AI 配置。

2. `*.onekeep-backup.zip`
   - 用于完整数据备份与恢复。
   - ZIP 内包含 JSON 数据、清单文件和用户记录中的本地照片。
   - 包含用户创建的计划、日程、训练历史、自定义动作、简短记录和非敏感设置。
   - 不包含 API Key、Keychain 内容、视频缓存、网页 Cookie、日志和临时文件。

完整备份目录结构：

```text
OneKeep-Backup/
  manifest.json
  data.json
  media/
    quick-logs/
    body-photos/
```

### 导入流程

选择文件 → 检查文件类型和格式版本 → 验证字段与数值范围 → 展示预览摘要 → 选择导入方式 → 原子写入。

导入规则：

- 所有记录使用稳定 UUID，避免重复导入产生无穷副本。
- 计划导入默认创建副本，不覆盖同名计划。
- 完整恢复提供“合并”和“替换本地数据”两种模式。
- 替换前自动创建一次本地恢复快照并进行二次确认。
- 任意一条关键数据校验失败时，不写入部分数据。
- 支持较旧格式迁移；不支持的新版本显示明确错误，不尝试猜测字段。
- 导入报告列出新增、更新、跳过和失败数量。

### 导出规则

- 使用 UTF-8 JSON 和固定日期格式。
- 根节点包含 `formatVersion`、`exportedAt`、`appVersion` 和 `payloadType`。
- 数据按稳定顺序输出，便于版本对比和问题排查。
- 导出前可查看数据范围与记录数量。
- 通过系统 Files/Share Sheet 保存，不上传到 OneKeep 服务器。
- 导出文件不写入任何 AI API Key。

### 完整性和安全

- 导出清单记录数据条数和 SHA-256 校验摘要。
- 导入限制文件大小、嵌套深度、字符串长度和集合数量，避免异常 JSON 导致卡顿或内存耗尽。
- 解压时拒绝绝对路径、`..` 路径穿越、符号链接和超出限制的压缩数据。
- `manifest.json` 记录每个媒体文件的相对路径、大小、类型和 SHA-256。
- 媒体文件名使用记录 UUID，不使用用户原始文件名。
- 外部视频 URL 只作为字符串导出，不下载或嵌入视频内容。
- 日志不记录完整导入内容，只记录脱敏后的错误位置。
